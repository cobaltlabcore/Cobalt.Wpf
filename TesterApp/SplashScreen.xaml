<ui:FluentWindow x:Class="TesterApp.SplashScreen"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:vm="clr-namespace:TesterApp.ViewModels"
                 mc:Ignorable="d"
                 WindowStartupLocation="CenterScreen"
                 ResizeMode="NoResize"
                 Height="250" Width="600"
                 d:DataContext="{d:DesignInstance vm:SplashScreenViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <TextBlock Text="Hello splash screen !!"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Grid>
        
        <StackPanel Grid.Row="1">
            <TextBlock Text="{Binding Message}"
                       Margin="10"
                       TextTrimming="CharacterEllipsis"/>
            <ProgressBar IsIndeterminate="{Binding IsIndeterminate}"
                         Value="{Binding Value}"
                         Height="10"/>
        </StackPanel> 
    </Grid>
</ui:FluentWindow>